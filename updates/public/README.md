# LostTrackr Update Public Key

`losttrackr_update_ed25519_public.pem` is the public key embedded in the app to
verify signed update manifests.

The matching private key must stay local, ignored by Git, and never be uploaded.
